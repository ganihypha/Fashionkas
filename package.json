{
  "name": "fashionkas",
  "type": "module",
  "version": "1.0.0",
  "description": "FashionKas - Kasir Digital + Katalog Online + WA Automation untuk Fashion Reseller Indonesia",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name fashionkas"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "vite": "^6.2.0",
    "wrangler": "^3.114.0",
    "typescript": "^5.7.3"
  }
}
